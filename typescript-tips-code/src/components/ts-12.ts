const obj = {
  id: 1233232,
  count: 5,
  isAvailable: true,
};

const colors = ["red", "green", "blue"];

let id = "123";
const username = "codelicks";
let bool = true;

type TypeOfObje = typeof obj;
type TypeOfColors = typeof colors;

type TypeOfId = typeof id;
type TypeOfUsername = typeof username;

type TypeOfBool = typeof bool;
